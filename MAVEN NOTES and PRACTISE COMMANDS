Jenkins vm - location hyderabad.

&&&&&&&&&&&&&&&&&&&&&&&&&----------MAVEN NOTES and PRACTISE COMMANDS-----------&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&

Install jdk
install maven

Build code process :-
compile
code review :-
unit test


code coverage.
package

======================================================================================================================================
### Dashboard - Manage Jenkins - System configuration - Tools -> : 

JDK Installation - 
Name - java
JAVA_HOME : /usr/lib/jvm/java-21-openjdk-amd64

Git Installation
Name : git
path : git

Maven Installation -
Name : Maven 
path : /opt/maven/bin/mvn 


---------------------------------------------------------------------------------------------------------------
df h  - check the size of the vm occupied.
java --version
mvn --version

git clone https://github.com/Alroyce/Java1.git  - cloning the project(https/ssh)

mvn validate              - check the build prerequisites.
mvn compile               - compiles the source code identified for the build.
mvn test                  - runs unit test for the compiled code.
mvn package               - assembles the compiled code into the binary build result.
mvn install               - shared the build result on the same machine.
mvn deploy                - publishes the build into the remote repository for the other projects to use.

ls -l target/

ls -ltrh                  - ists the contents of a directory in a long-listing format, sorted by modification time in reverse order 
                           ( oldest first, newest last), with human-readable file sizes.


if any steps fails, then we can contact the developer.

mvn compile               - compiling.
mvn test                  - testing the code.
mvn package               - complete code.

continously compiling     -    

-------------------------------------------------------------------------------------------------------------------------

Stage 1 - compile 

select project : Freestyle
description : Compiles the Maven project source code and validates successful compilation.
souce code management : https://github.com/Alroyce/Java1.git
to specify the creditails (for private repo: use username and password)
Branch Specifier : Main/master/defect/preprod/test1
Build Steps --> Execute shell : /opt/maven/bin/mvn compile/test/verify/package.
Post-build Actions --> None
------------------------------------------------------------------------------------
Stage 2 - code review

desc: Performs code quality and static analysis checks to identify coding issues, 
      vulnerabilities, and maintainability concerns 
Build Steps --> Execute shell : /opt/maven/bin/mvn -P metrics pmd:pmd
Post-build Actions --> None

====||==== Result : The generated result will be in WORKSPACE :  /var/lib/jenkins/workspace/spinbuild1-verify ( use in mobaxterm)

Building in workspace /var/lib/jenkins/workspace/spinbuild1-verify
cd /var/lib/jenkins/workspace/spinbuild1-verify
ls
cd target
ls
cat pmd.xml - non readable format so use plugin to convert the code to readable format.

== Plugins - Warning plugins
Jenkins- Manage jenkins - plugins - WARNINGS - install.

Re Configure the Job - and add 
POST BUILD ACTIONS : Record compiler -> tool -> PMD --> Report File Pattern --> **/pmd.xml.

INFO : if there is no internet then download and upload in the file format 
download the direct file -> manage jenkins -> plugins - Advanced settings -> choose file (choose the selected file )

------------------------------------------------------------------------------------
Stage 3 - unittest

desc: Executes automated unit tests to verify individual components and ensure the application functions correctly without test failures.
Build Steps --> Execute shell : /opt/maven/bin/mvn test
Post-build Actions --> Publish JUnit test results – Display unit-test results in Jenkins.

--------------------------------------------------------------------------------------
Stage 4 - Code Coverage

desc: Executes the Maven verify phase to validate the build, run configured tests, and generate code coverage reports to measure test coverage.
Build Steps --> Execute shell : /opt/maven/bin/mvn verify
Post-build Actions --> Publish JUnit test results – Display unit-test results in Jenkins.

Re Configure the Job - and add 
POST BUILD ACTIONS : Record JaCoCo coverage report.

Check the Coverage code - (more should be green)


========================================================================================










@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@------ Questions and Answers ------!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!

what is maven - Maven is a build tool which operates on POM ( Page Object model ) which can manage project build, reporting and documentation from central piece of information.
What is POM - POM Stands for Page object Model.
Jenkins - Similar tools ==== Jenkins, AWS CODE BUILD, Gitlab CI, Github Actions.

What is Jenkins - Jenkins is a tool that helps us to automate the build code process and also as build automation server.
Jenkins PORT - 8080

Jenkins
What is Jenkins and why is it used? 
What is the difference between a Freestyle project and a Pipeline?
What is a Jenkins controller and agent?
What is the Jenkins built-in node?
Why should builds generally run on agents instead of the controller?
How do you configure a Freestyle project?
What are Source Code Management (SCM) options in Jenkins?
How do you integrate Jenkins with Git?
What are build triggers in Jenkins?
What is the difference between Build Now, Poll SCM, and Webhook?
What are Jenkins environment variables?
How do you pass parameters to a Jenkins job?
What is a Jenkins workspace?
How do you clean the Jenkins workspace?
How do you archive artifacts in Jenkins?
What is the difference between an artifact and a build?
How do you configure Maven in Jenkins?
What is the purpose of pom.xml?
What is the difference between mvn compile, mvn test, mvn package, and mvn install?
How do you troubleshoot a failed Jenkins build?
What happens when Jenkins reports "Disk space is below threshold"?
How do you bring an offline Jenkins node back online?
How do you manage Jenkins credentials?
How do you restrict who can execute a Jenkins job?
How do you configure email/Slack notifications for failed builds?
Maven
What is Maven?
What is the purpose of pom.xml?
Explain the Maven lifecycle.
What is the difference between:
compile
test
package
install
deploy
What are Maven dependencies?
What is a Maven repository?
Difference between local, central, and remote repository?
What is dependency management?
What is the difference between dependency and dependencyManagement?
What are Maven plugins?
What is the Maven Surefire plugin?
How do you skip tests in Maven?
How do you run a specific test?
How do you troubleshoot Maven dependency errors?
Git
What is Git?
Difference between Git and GitHub?
Explain git clone, git pull, git fetch.
Difference between git merge and git rebase.
What is a Git branch?
How do you create and delete a branch?
What is a merge conflict and how do you resolve it?
What is git stash?
Difference between git reset and git revert.
What is .gitignore?
How do you check commit history?
How do you undo the last commit?
Linux
For 2 years of DevOps experience, expect practical Linux questions:

How do you check disk usage?
Difference between df and du.
How do you find a large file?
How do you check running processes?
Difference between kill and kill -9.
How do you check CPU and memory usage?
How do you search for a string inside files?
Difference between > and >>.
What are Linux file permissions?
Explain chmod 755.
How do you check the status of a service?
How do you view logs?
What is a shell script?
How do you schedule a job using cron?
Docker
What is Docker?
Difference between a Docker image and container.
What is a Dockerfile?
Explain common Dockerfile instructions.
Difference between CMD and ENTRYPOINT.
What is a Docker volume?
What is a Docker network?
How do you check running containers?
How do you view container logs?
How do you enter a running container?
How do you build and run an image?
How do you reduce Docker image size?
Scenario-based questions
These are especially important at the 2-year level:

Jenkins build suddenly fails. How will you troubleshoot it?
Jenkins says disk space is below threshold. What will you check?
Maven build works locally but fails in Jenkins. What could be the reason?
Jenkins cannot clone the Git repository. How will you troubleshoot?
A Maven test is failing. How do you identify the failed test?
Build is successful but deployment fails. What will you check?
Jenkins job is taking much longer than usual. How do you investigate?
A Docker container keeps restarting. How will you troubleshoot it?
The application works on one server but not another. What will you check?
How would you design a Jenkins flow: Git → Compile → Test → Package → Deploy?
A very common practical question
They may give you a situation like:

"Create a Jenkins Freestyle project that gets code from Git, compiles it using Maven, runs tests, and archives the generated artifact."

You should be comfortable explaining:

Git → Jenkins Workspace → Maven Compile → Maven Test → Maven Package → Archive Artifact





